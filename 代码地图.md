# 代码地图

本文档记录 BankBin 当前代码结构、关键职责和运行时文件路径约束。后续涉及路径、程序名、日志、时间格式、打包行为的修改，应先更新本文档。

## 总体目标

- 程序名称统一为 `BankBin`。
- GitHub 仓库统一为 `mountopjh/BankBin`。
- EXE 同目录只作为只读资源位置，不产生运行时文件。
- 运行时数据库、配置、崩溃日志统一写入 `%APPDATA%\BankBin`。
- 程序涉及的日志和界面时间展示精确到秒，格式统一为 `YYYY-MM-DD HH:MM:SS`。
- 登录窗口仅作为本地入口，不连接后台、不校验账号密码。
- 本地配置只保存最近账号和账号历史，不保存密码或后台登录令牌。
- 旧 `bmob_client.py` 已删除，Bmob 不再作为运行时认证、崩溃上报或新增 BIN 上传入口。

## 核心文件

### `app_paths.py`

职责：
- 定义应用目录名：`APP_DIR_NAME = "BankBin"`。
- 识别运行目录：
  - 打包后：`RUN_DIR = os.path.dirname(sys.executable)`。
  - 源码运行：`RUN_DIR = os.path.dirname(os.path.abspath(__file__))`。
- 识别 PyInstaller onefile 临时资源目录：`BUNDLE_DIR`。
- 解析 AppData 保存目录：`APP_DATA_DIR`。
- 统一导出运行时写入路径：
  - `DB_PATH = %APPDATA%\BankBin\bin_database.db`
  - `SETTINGS_PATH = %APPDATA%\BankBin\settings.json`
  - `CRASH_LOG_PATH = %APPDATA%\BankBin\crash_report.log`
- 保留旧目录只读迁移来源：
  - `%APPDATA%\Jixiaohe\bin_database.db`
  - `%APPDATA%\Jixiaohe\settings.json`
  - EXE 同目录旧 `bin_database.db`
  - EXE 同目录旧 `settings.json`

边界：
- 可以创建 `%APPDATA%\BankBin`。
- 不允许把运行时文件写入 EXE 同目录。
- 旧 `Jixiaohe` 名称只允许作为迁移兼容常量存在，不应出现在用户可见 UI 和文档叙述中。

### `settings_manager.py`

职责：
- 读取和保存 `settings.json`。
- 优先读取 `SETTINGS_PATH`。
- 如果新配置不存在，则按顺序只读迁移：
  - 旧 AppData 配置路径。
  - EXE 同目录旧配置路径。
- 保存配置时使用 AppData 内临时文件，再 `os.replace` 到 `SETTINGS_PATH`。
- 登录配置字段：
  - `username`：最近使用的账号。
  - `login_history`：最近账号列表，不保存密码。

边界：
- 不删除旧配置。
- 不写 EXE 同目录。
- 如果旧配置损坏，返回空配置，避免启动失败。
- 不允许继续保存 `password` 或历史密码。

### `data_manager.py`

职责：
- 初始化并连接 SQLite 数据库。
- 确保本地数据库存在且可写。
- 数据库主路径来自 `DB_PATH`。
- 新数据库不存在时，按顺序初始化：
  - 从旧 AppData 数据库只读迁移。
  - 从 EXE 同目录旧数据库只读迁移。
  - 从 onefile 资源目录内置 `bin_database.db` 复制。
  - 从源码目录开发种子库复制。
  - 最后创建空数据库。
- 负责查询历史表和 BIN 数据表 schema。

边界：
- 运行时写入只发生在 `%APPDATA%\BankBin`。
- `BANK_DIR = RUN_DIR\bank2025.2` 仅用于开发场景读取 Go 种子文件。
- `query_time DATETIME DEFAULT CURRENT_TIMESTAMP` 由 SQLite 生成，精确到秒。

### `crash_reporter.py`

职责：
- 捕获未处理异常。
- 崩溃日志写入 `CRASH_LOG_PATH`。
- 崩溃时间格式为 `YYYY-MM-DD HH:MM:SS`。
- 云端崩溃上传暂不启用，只保留本地日志。

边界：
- 崩溃日志不使用相对路径。
- 崩溃日志写入失败时静默跳过，避免异常处理器二次崩溃。

### `main.py`

职责：
- PyQt 主程序、托盘菜单、登录、更新检查、监听服务、窗口和弹窗调度。
- 登录界面在用户点击“登录”后直接放行，不调用任何后台认证服务。
- 程序显示名常量：`APP_NAME = "BankBin"`。
- 单实例互斥锁：`BankBin_SingleInstance_2026`。
- 数据库同步写入 `DB_PATH` 所在目录。
- 所有程序日志/诊断时间通过 helper 格式化到秒：
  - `format_now_seconds()`
  - `format_timestamp_seconds(timestamp)`
  - `format_iso_seconds(value)`

时间相关位置：
- 加载窗口日志：`[YYYY-MM-DD HH:MM:SS]`
- 监听诊断日志：`[YYYY-MM-DD HH:MM:SS]`
- 最近点击时间：`YYYY-MM-DD HH:MM:SS`
- GitHub BIN 更新提交时间：`YYYY-MM-DD HH:MM:SS`

GitHub 更新相关：
- 版本检查地址：`https://api.github.com/repos/mountopjh/BankBin/releases/latest`。
- 软件启动时检查一次，运行期间每 5 分钟检查一次。
- 发现更高版本时，在系统托盘“更新”菜单显示版本号并弹出通知。
- 点击版本更新菜单项会打开 Release 中的 EXE 附件；没有 EXE 附件时打开 Release 页面。
- BIN 数据库提交检查和原始数据库下载同样使用 `mountopjh/BankBin`。

用户可见程序名位置：
- 重复启动提示标题。
- 登录弹窗标题文本。
- 主窗口标题。
- 托盘 tooltip。
- 托盘图标中心文字：`Bin`。
- 关于菜单。

登录相关约束：
- 登录窗口显示账号和密码，并预填默认值。
- 只检查账号和密码非空，不与后台数据匹配。
- 不保存密码；启动时清理旧配置中的明文密码及 Firebase 令牌字段。
- 不提供修改密码入口；切换账号会重新显示本地登录窗口。

### `ui_popup.py`

职责：
- 提供 `ResultPopup` 查询结果弹窗。
- 支持 `show_result(card_number, record, cursor_pos)`。
- 支持 `dismiss()` 关闭弹窗。
- 网络查询中状态和最终查询结果共用同一个弹窗入口。

边界：
- 不写入任何文件。
- 只展示 `query_engine.perform_full_query` 返回的数据。

### `panels/registry.py`

职责：
- 构建主面板注册表。
- 对 `main.py` 提供：
  - `build_default_registry(signal_sender)`
  - `get_primary_widget()`
  - `panel_ids()`
  - `get_panel(panel_id)`
  - `get_primary_id()`
  - `refresh_visible_panels()`

边界：
- 只负责面板组织，不直接做查询业务。

### `panels/bin_query_panel.py`

职责：
- 提供主查询窗口。
- 支持手动输入银行卡号或 BIN。
- 查询完成后通过 `signal_sender.show_popup_signal` 展示弹窗。
- 展示最近查询历史。

边界：
- 查询业务调用 `query_engine.perform_full_query`。
- 历史读取调用 `query_engine.get_query_history`。

### `query_engine.py`

职责：
- 本地数据库优先查询 BIN。
- 网络兜底使用支付宝接口和 cardbin.cn。
- 新查到的 BIN 写入本地 SQLite。
- 写入查询历史。

边界：
- 不再上传新增 BIN 到 Bmob。
- 不写 EXE 同目录。

### `build_root_exe.bat`

职责：
- 创建或复用 `.build_venv`。
- 安装依赖和 PyInstaller。
- 验证关键依赖导入。
- 使用 PyInstaller onefile/windowed 打包。

当前产物：
- `BankBin_Releases\BankBin_001.exe`、`BankBin_Releases\BankBin_002.exe`……
- 输出统一保存在项目根目录下的 `BankBin_Releases` 文件夹。
- 序号根据现有历史版本的最大编号自动递增。
- `build` 目录为构建缓存。

边界：
- 脚本仅清理 `build` 构建缓存，不删除或覆盖 `BankBin_Releases` 中的历史 EXE。
- PyQt6/Qt 使用 `requirements.txt` 中固定的兼容版本，避免构建时自动升级导致目标电脑 DLL 不兼容。
- 不再生成 `Jixiaohe.exe`。

### `BankBin.spec`

职责：
- PyInstaller spec 文件。
- EXE 名称由构建脚本通过 `BANKBIN_BUILD_NAME` 环境变量设置；未设置时回退为 `BankBin`。
- 内置资源包括 `bin_database.db` 和 Markdown 文档。

边界：
- `datas` 内的文件是打包资源，运行时应只读使用。

### `README.md`

职责：
- 项目说明和用户使用说明。
- 当前叙述使用 `BankBin`。
- 明确运行时文件保存到 `%APPDATA%\BankBin`。

## 运行时文件流向

```text
BankBin.exe 同目录
  只读资源来源：
    - bin_database.db
    - 旧 settings.json（只读迁移）
    - 旧 bin_database.db（只读迁移）

%APPDATA%\BankBin
  运行时写入：
    - bin_database.db
    - settings.json
    - crash_report.log
    - 数据库同步临时文件
    - 配置保存临时文件

%APPDATA%\Jixiaohe
  兼容读取：
    - 旧 bin_database.db
    - 旧 settings.json
```

## 禁止事项

- 禁止新增 `open("xxx.log", "w/a")` 这类相对路径写入。
- 禁止新增 EXE 同目录运行时写入目录，例如 `_data`、`logs`、`cache`。
- 禁止把新配置、新数据库、新日志保存到 `RUN_DIR`。
- 禁止用户可见叙述继续使用旧程序名。
- 禁止新增不精确到秒的时间展示。
- 禁止在 `settings.json` 保存明文密码。
