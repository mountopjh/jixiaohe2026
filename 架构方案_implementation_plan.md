# BIN码查询工具实施计划

## 目标简述
开发一款带现代化用户界面（基于 PyQt6）的 Windows 本地化应用，主要功能为结合 WPS 电子表格实现快捷键划词/选单元格读取并查询银行的 BIN 码信息。应用支持三种维度的查询：本地数据库查询 -> Alipay API 兜底 -> cardbin.cn 爬虫兜底，并自带历史记录与导入导出功能。环境完全本地化（虚拟环境）。

## 需要用户审核的内容

> [!CAUTION]
> 在正式编写代码前，有几个关键技术细节需要您的确认（已通过对话框向您提问）。特别是快捷键的冲突问题、WPS 的交互方式（COM API 或 模拟剪贴板）以及初次构建本地数据库的数据源（Excel 或 Go源码）。

## 架构及更改说明

### 1. 核心应用架构 (Python)
- **UI 框架**: 使用 `PyQt6`。它原生支持：
  - 系统托盘（System Tray）图标与右键菜单。
  - 现代化、可自定义的、无边框悬浮弹窗（用于展示 BIN 码结果）。
  - 美观的表格和主面板界面（用于查询历史记录）。
- **本地存储**: 使用本地 `SQLite` 数据库 (`bin_database.db`) 用于存储本地 BIN 码库以及历史查询记录。相较于每次读取 Excel，SQLite 查询极快且支持高频读写。

### 2. BIN码库云端与本地同步方案（Bmob 集成）
- 首次运行时，扫描 `g:\23 测试bin\bank2025.2` 下的数据源（推荐使用其中的 `.xlsx` 文件）。
- 提取数据存入本地 SQLite。
- 引入 Bmob 云开发 REST API/SDK：
    - 加入应用打开时的账号鉴权登录管控。
    - 后续如果通过网络（Alipay 或 cardbin.cn）查到任何本地库中没有的新 BIN 码，自动双写：不仅存到本地 SQLite，同时静默 POST 到 Bmob 云端表，实现云端“众包库”扩展。

### 3. 热键绑定与 WPS 取词逻辑整合
- 使用 `keyboard` 或 `pynput` 库注册系统级全局快捷键（如 `ALT` 或指定的其他按键）。
- **WPS 交互方式**（推荐方案）：当在 WPS 中选中单元格并按下快捷键时，程序通过发送虚拟键 `Ctrl+C` 将选中的内容存盘到系统剪贴板，程序瞬间读取剪贴板内容，再从中提取数字（过滤掉空格、连字符等）。这种方案比调用 COM 接口更稳定且适用性广。

### 4. 三级查询机制实现
1. **本地数据库 (`SQLite`)**: 极速匹配。
2. **Alipay API**: 如果本地无，且长度符合完整银行卡号规则（通常 >= 15 位数字），请求 `https://ccdcapi.alipay.com/validateAndCacheCardInfo.json?cardNo=...&dBinCheck=true`。
3. **Cardbin.cn 爬虫**: 如果 Alipay 查不到或者只有部分 BIN 码（非完整卡号的 6-8 位），使用 `playwright` (无头浏览器) 后台访问查询页面提取结果。
- 无论哪种渠道查到的有效结果，均展现至悬浮弹窗，并：(1)入库 BIN DB，(2) 记录日志至历史表格。

### 5. UI 组件分离
- `main.py`: 入口文件及系统托盘逻辑。
- `ui_panel.py`: 主面板 UI（历史记录表格、导出按钮、统计信息）。
- `ui_popup.py`: 精致的悬浮弹窗 UI，负责展示单次查询结果，带淡入淡出动画并支持点击关闭。
- `data_manager.py`: 负责 SQLite 操作、历史记录存取及文件解析。
- `query_engine.py`: 负责发起支付宝 API 与爬虫兜底的方法封装。

## 测试与验证计划

### 自动化/半自动化验证
- **本地单元测试**: 编写轻量级脚本验证 `g:\23 测试bin\bank2025.2` 文件解析能否正确输出至 SQLite。
- **爬虫稳定性测试**: 单独针对 Alipay API 和 cardbin.cn 运行几个示例卡号（完整/非完整），验证网络请求及数据解析是否正确。

### 手动功能验收
1. 启动 `启动程序.bat`（进入虚拟环境并启动应用），确认托盘图标正常显示，右键菜单的“开启/关闭”状态有效。
2. 打开 WPS 或任意文本编辑器，选中一串由数字组成的文本，按下约定的快捷键，验证能否弹出精致悬浮窗并正确显示银行卡和归属行信息。
3. 打开主界面（命令面板），检查历史查询记录是否实时罗列，操作导出按钮能否正常输出最新的 `.csv` 库文件。
