# 规划地图

本文档记录 BankBin 当前改造目标、已完成事项、后续修改流程和验证清单。后续任何路径、程序名、日志、时间格式、打包相关修改，应先阅读并同步更新本文档。

## 当前目标

1. 程序统一命名为 `BankBin`。
2. EXE 运行时不在 EXE 同目录生成任何文件。
3. 必须保存的数据统一放到 `%APPDATA%\BankBin`。
4. 旧数据只读迁移，不能写回旧目录或 EXE 同目录。
5. 程序涉及的日志、诊断和提示时间精确到秒。
6. 打包产物统一保存到 `BankBin_Releases`，名称按序号递增且不覆盖历史版本。
7. 登录窗口仅作为本地入口，不连接后台、不匹配账号密码。
8. 本地不保存明文密码或后台认证令牌。

## 已完成事项

### GitHub 仓库与版本更新（2026-09-07）

- GitHub 仓库统一切换为 `mountopjh/BankBin`。
- 软件版本更新与 BIN 数据库更新均使用新仓库。
- 启动时及运行期间定时检查仓库中的 `update_manifest.json`；GitHub Releases API 仅作为后备。
- 定时检查发现新版本时在系统托盘“更新”菜单显示版本号但不自动安装；用户点击“立即检查更新”后，发现新版即静默下载、校验、退出旧程序、替换原 EXE 并启动新版本。
- 打包 EXE 通过 GitHub Release 附件发布，构建缓存和历史本地 EXE 不提交源码仓库。
- 后续可将代码同步、版本检查和安装包下载源统一迁移至 GitCode：只需将更新清单地址和清单中的下载地址切换至 GitCode，保持相同的一键更新流程。

### 本地登录入口（2026-09-07）

- Firebase 登录、自动登录、令牌刷新和修改密码功能已移除。
- 登录窗口继续保留，账号密码非空时点击“登录”直接进入软件。
- 配置不保存密码，并清理旧 Firebase 令牌字段。
- `firebase_auth_client.py` 已删除，不再打包后台认证客户端。

### 程序名

- `main.py` 增加 `APP_NAME = "BankBin"`。
- 重复启动提示标题改为 `BankBin`。
- 登录标题改为 `BankBin`。
- 主窗口标题改为 `BankBin - 当前账号：...`。
- 托盘 tooltip 改为 `BankBin`。
- 托盘图标中心文字改为 `Bin`。
- 关于菜单改为 `关于 - BankBin ...`。
- 单实例互斥锁改为 `BankBin_SingleInstance_2026`。
- README 叙述改为 `BankBin`。

### 路径收口

- 新运行时目录：`%APPDATA%\BankBin`。
- 数据库路径：`%APPDATA%\BankBin\bin_database.db`。
- 配置路径：`%APPDATA%\BankBin\settings.json`。
- 崩溃日志路径：`%APPDATA%\BankBin\crash_report.log`。
- 数据库同步临时文件写入 `DB_PATH` 所在目录。
- 配置保存临时文件写入 AppData 目录。

### 旧数据迁移

- 旧 `%APPDATA%\Jixiaohe` 只作为迁移来源。
- EXE 同目录旧 `bin_database.db` 只作为迁移来源。
- EXE 同目录旧 `settings.json` 只作为迁移来源。
- 迁移时复制到 `%APPDATA%\BankBin`，不删除旧文件，不写回旧位置。

### 时间格式

- 加载日志改为 `YYYY-MM-DD HH:MM:SS`。
- 监听诊断日志改为 `YYYY-MM-DD HH:MM:SS`。
- 监听最近点击时间改为 `YYYY-MM-DD HH:MM:SS`。
- GitHub 提交时间改为 `YYYY-MM-DD HH:MM:SS`。
- 崩溃日志时间改为 `YYYY-MM-DD HH:MM:SS`。
- SQLite `CURRENT_TIMESTAMP` 本身精确到秒，暂不改 schema。

### 打包

- `Jixiaohe.spec` 已改名为 `BankBin.spec`。
- spec 内 EXE 名称改为 `BankBin`。
- `build_root_exe.bat` 输出 `BankBin.exe`。
- 旧 `build` 目录是构建缓存，未手动修改；重新打包会刷新。
- `requirements.txt` 提供打包依赖清单。
- `ui_popup.py`、`panels/registry.py`、`panels/bin_query_panel.py` 已补齐，避免 PyInstaller 缺项目模块。

### 历史：Firebase Auth 迁移计划（已停用）

以下内容仅记录过去的迁移过程，当前版本已改为不连接后台的本地登录入口。

- 新增 `firebase_auth_client.py`。
- `main.py` 登录、自动登录、退出、当前账号、修改密码改为调用 Firebase 客户端。
- 登录成功后保存邮箱、历史邮箱、Firebase token，并移除旧 `password` 字段。
- 登录窗口不再从历史记录填充密码。
- 崩溃日志暂时只写本地，不再上传 Bmob。
- 网络查询新发现 BIN 只保存本地，不再上传 Bmob。
- 旧 `bmob_client.py` 删除，避免保留废弃服务凭据和误用入口。
- README 中的云端账号说明改为 Firebase Auth。

## 后续修改流程

1. 先更新代码地图和规划地图。
2. 用 `rg` 扫描相关落点。
3. 修改代码时保持改动范围最小。
4. 所有新增运行时文件路径必须通过 `app_paths.py`。
5. 所有新增程序时间展示必须精确到秒。
6. 所有新增用户可见程序名必须使用 `BankBin`。
7. 修改后运行静态扫描和语法检查。
8. 如果依赖齐全，再运行或打包验证。

## 验证清单

### 程序名残留

运行：

```powershell
rg -n -g "!build/**" -g "!__pycache__/**" -g "!**/__pycache__/**" -g "!.venv/**" -g "!.build_venv/**" "Jixiaohe|纪小盒|NJXiaohe|小盒" .
```

预期：
- 只允许出现 `app_paths.py` 中的旧目录迁移兼容常量。

### 新程序名

运行：

```powershell
rg -n -g "!build/**" -g "!__pycache__/**" -g "!**/__pycache__/**" -g "!.venv/**" -g "!.build_venv/**" "BankBin|BankBin.exe|BankBin_SingleInstance|%APPDATA%\\BankBin" .
```

预期：
- UI、文档、打包脚本、spec、路径常量都使用 `BankBin`。

### 运行时路径

运行：

```powershell
python -B -c "import app_paths; print(app_paths.APP_DATA_DIR); print(app_paths.DB_PATH); print(app_paths.SETTINGS_PATH); print(app_paths.CRASH_LOG_PATH)"
```

预期：
- 输出路径均位于 `%APPDATA%\BankBin`。

### 相对路径写入

运行：

```powershell
rg -n "open\(|mkstemp|NamedTemporaryFile|write_text|write_bytes|json.dump|sqlite3.connect|os.makedirs|shutil.copy|os.replace|FileHandler" . -g "*.py"
```

重点确认：
- 写数据库只写 `DB_PATH`。
- 写配置只写 `SETTINGS_PATH`。
- 写崩溃日志只写 `CRASH_LOG_PATH`。
- 临时文件目录位于 AppData。
- EXE 同目录路径只读迁移，不写入。

### 时间精确到秒

运行：

```powershell
rg -n -g "!build/**" -g "!__pycache__/**" -g "!**/__pycache__/**" -g "!.venv/**" -g "!.build_venv/**" "strftime|datetime.now|CURRENT_TIMESTAMP|format_now_seconds|format_timestamp_seconds|format_iso_seconds" .
```

预期：
- 展示和日志时间使用 `YYYY-MM-DD HH:MM:SS`。
- 不再使用只有 `%H:%M` 的分钟级时间。

### 语法检查

运行：

```powershell
python -m py_compile app_paths.py settings_manager.py data_manager.py crash_reporter.py main.py query_engine.py
```

预期：
- 无输出，退出码为 0。

### 本地登录链路

运行：

```powershell
rg -n -g "!build/**" -g "!__pycache__/**" -g "!**/__pycache__/**" -g "!.venv/**" -g "!.build_venv/**" "firebase_auth_client|login_with_status|refresh_session|change_password_with_status" *.py panels
```

预期：
- 运行时代码不引用 `firebase_auth_client.py` 或 Firebase 登录方法。
- `settings.json` 写入逻辑不保存明文 `password`，并清理旧 Firebase 令牌。
- `Bmob` 只允许出现在本规划历史说明和迁移风险描述中。

## 风险点

- `build` 目录内仍可能存在旧 PyInstaller 缓存文本，重新打包后会刷新。
- `OLD_APP_DIR_NAME = "Jixiaohe"` 是必要兼容项，不能简单删除，否则旧用户数据不会自动迁移。
- 如果用户手动把 EXE 放在无权限目录，程序仍应写入 AppData，而不是回退写 EXE 同目录。

## 后续建议

- 下次打包后检查根目录是否生成 `BankBin.exe`。
- 运行一次 EXE，确认 EXE 同目录没有新增文件。
- 检查 `%APPDATA%\BankBin` 是否生成数据库、配置和崩溃日志。
