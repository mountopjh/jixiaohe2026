# 项目验收指南：BIN码查询工具

本项目旨在实现一个通过快捷键（如鼠标选中数字后按下 `ALT` 键，或 `Ctrl+Alt+C` 防冲突备用快捷键）实现全局/WPS 电子表格取词，并快速分析其银行 BIN 码信息的高效工具。所有的依赖及运行环境完全本地化。

### :rocket: 核心功能实现
*   **三重保障查询机制**
    1.  **极速 SQLite 数据库查询** (将 `bank2025.2` 中的本地发卡行数据无缝注入 `bin_database.db`，支持百万发查，毫秒级响应)。
    2.  **支付宝官方 API 校验兜底** (`ccdcapi.alipay.com`)：精准匹配完整的银行卡（>= 15 位数字）。
    3.  **`cardbin.cn` 网络爬虫兜底**：通过无 UI 版的 Chromium 浏览器技术 (Playwright) 提取复杂或最新发卡行的 BIN。
*   **历史查询自动入库功能**：凡是在互联网上新查到的银行 BIN 信息，都会被程序悄悄收藏进入 `bin_database.db`。您越用，本地库越完善！
*   **漂亮且灵动的 UI**：
    *   **悬浮动画弹窗**：在您的鼠标选区附近以流畅的**淡入淡出动画**出现，查询完成即刻消失！
    *   **历史记录面板**: 您可以在任何时候通过双击系统托盘（右下角）打开面板，查看查询轨迹，并一键**全量导出最新的本地 BIN 库为 `.csv`。**

### :open_file_folder: 项目结构
*   `main.py`：系统托盘挂载、键盘按键录制 Hook。
*   `query_engine.py`：查询总线逻辑、Alipay与无头浏览器爬虫方法。
*   `data_manager.py`：SQLite 操作、本地初始化载入、解析。
*   `ui_panel.py`：暗黑系高级质感的历史大屏面板。
*   `ui_popup.py`：精美悬浮浮窗（无边框，带有发光边框，支持拖曳消除）。
*   `requirements.txt`：项目运行必须的包集。
*   `bin_database.db`：应用启动时依据底层 Go 文件库自动生成的本地 SQL 库 (内含 1709 个全国行名及缩写)。

### :bulb: 运行与使用方法

1. **环境已就绪**
   我已经帮您在 `g:\23 测试bin\venv` 虚拟环境中把所有的依赖包（包括大型的 `PyQt6`、`playwright`无头内核、`keyboard` 钩子）全部安装且配置好了。
   
2. **如何启动？**
   去 `g:\23 测试bin` 目录中寻找您之前准备的 **`启动程序.bat`** 脚本，双击运行即可。

3. **魔法选词划词**
   程序启动后，右下角任务栏出现蓝色的 **"BIN"** 图标。选中数字后按下 **系统全局快捷键 (默认为 `Ctrl+Alt+Z`)** 松开，系统便会进行复制、解析、查询，弹窗展示。
   
4. **自定义与历史 (V1.4)**
   * **改键**：在托盘图标上右键，点击【设置】，可修改并永久记忆快捷键。
   * **快捷历史**：托盘右键的【历史搜索记录】中鼠标一悬停，立马展示前 20 条。
   * **手动干预面板**：双击托盘图标打开黑曜石大屏控制台，不仅能随时看到所有历史细节，还能直接利用上方的输入框 `[手动输入卡号或BIN码查询...]` 粘贴查询！
   * **绝对置顶浮窗**：浮窗弹出时处于 Windows 系统的最上层绝对无敌状态 (TopMost)，按此窗时您可以用鼠标直接去框内复制信息，不理会经过 10 秒则自然消散，也可以按 `ESC` 或者右上角的×光速关闭。
✅已完成本地初始化，自动发现 `bin.go` 与 `name.go` 内含数据：提取出 1700+ 条记录入本地 SQL。
✅应用与主 UI 加载成功：系统热键线程能无阻塞运行。
✅网络层 `playwright` 内核已缓存安装完成，查询引擎随时可唤醒 Chromium。
